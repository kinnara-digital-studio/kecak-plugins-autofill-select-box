(function($){
    $.fn.extend({
        autofillSelectBox : function(mapping) {
            $selectBox = $(this);

            // TODO: move from FTL to this block

        },

        trigger : function() {
            // TODO: move from FTL to this block
        }
    });
})(jQuery);